package com.levelup.journey.platform.community.post.domain.model.queries;

/**
 * Query to retrieve all non-deleted posts.
 * Posts are ordered by creation date (newest first).
 */
public record GetAllPostsQuery() {
}
